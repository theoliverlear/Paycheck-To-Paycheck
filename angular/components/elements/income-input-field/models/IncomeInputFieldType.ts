export enum IncomeInputFieldType {
    // TODO: Abstract into a more common enum given it's a match with
    //       BillInputFieldType.ts.
    TITLE = 'Title',
    AMOUNT = 'Amount',
    HOURS = 'Hours',
    DATE = 'Date',
}